# Fully pinned, cross-platform Python 3.11 environment for the default gate.
# It deliberately omits the OpenAI SDK and GUI-enabled OpenCV. Runtime-only
# dependencies remain in requirements.txt.
colorama==0.4.6
iniconfig==2.3.0
numpy==2.2.6
opencv-python-headless==4.12.0.88
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
pytest==9.0.1
